services:
  web:
    build: .
    environment:
      - DB_URL=${DB_URL}
      - PORT=${PORT:-5001}
    expose:
      - "5001"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5001/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s
